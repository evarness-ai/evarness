!!! tip "See it before you install it"
    The [live demos](demos.md) are real render artifacts generated by the CLI
    when this site was built — a replay you can scrub, a blocked run stopping
    before the model, and a proof browser carrying a bundle you can extract
    and verify offline. The build fails if their digests don't reproduce.

--8<-- "README.md"
