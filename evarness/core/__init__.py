"""The Evarness kernel: graphs, events, digests, contracts, proofs.

Domain-agnostic by rule — this package never imports ``evarness.domains`` or
``evarness.io``. Domains extend it through ``evarness.core.registry``.
"""
